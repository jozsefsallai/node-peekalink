/**
 * Contains information about the availability of a link.
 * @typedef {object} LinkAvailability
 * @property {boolean} [isAvailable] - Will be true if the given URL is available
 */
export interface LinkAvailability {
  isAvailable: boolean;
}
